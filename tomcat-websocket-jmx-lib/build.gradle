// This is a sub-project

project.ext.title = 'Tomcat WebSocket + JMX Demo lib'
project.ext.description = 'A description stub for Project lib'
project.ext.version = '0.1-SNAPSHOT'
project.ext.packageName = 'org.pidster.tomcat.websockets.jmx'

jar {
        archiveName = "${packageName}-${version}.jar"
        manifest {
                attributes (
                        'Implementation-Title': "$title",
                        'Implementation-Version': "$version"
                )
        }
}
