// This is a sub-project

project.ext.title = 'Tomcat WebSocket + JMX Demo webapp'
project.ext.description = 'A description stub for Project webapp'
project.ext.packageVersion = '0.1-SNAPSHOT'
project.ext.packageName = 'tomcat-websocket-jmx'

war {
        archiveName = "${packageName}##${packageVersion}.war"
        manifest {
                attributes (
                        'Implementation-Title': "$title",
                        'Implementation-Version': "${packageVersion}"
                )
        }
}
