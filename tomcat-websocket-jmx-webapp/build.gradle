// This is a sub-project

project.ext.title = 'Project webapp'
project.ext.description = 'A description stub for Project webapp'
project.ext.version = '0.1-SNAPSHOT'
project.ext.packageName = 'tomcat-websocket-jmx'

war {
        archiveName = "${packageName}##${version}.war"
        manifest {
                attributes (
                        'Implementation-Title': "$title",
                        'Implementation-Version': "$version"
                )
        }
}
